language:
  - cpp

compiler:
  - gcc

before_install:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test

install:
  - sudo apt-get install -qq gcc-6 g++-6
  - export CXX="g++-6"

script:
  - make
