language:
  - cpp

compiler:
  - gcc

before_install:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test

install:
  - sudo apt-get install g++-6 -y
  - export CXX="g++-6"
  - gcc --version

script:
  - make
